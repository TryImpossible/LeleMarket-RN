import Form, { FormProps } from './Form';
import FormItem, { FormItemType, FormItemProps } from './FormItem';

export type { FormProps, FormItemType, FormItemProps };
export default Form;
export { FormItem };
