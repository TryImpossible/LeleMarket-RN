import Alert, { AlertProps, AlertHandles } from './Alert';
import Layout, { LayoutProps, HeaderLayoutProps, BodyLayoutProps, NormalLayoutProps } from './Layout';

export type { AlertProps, AlertHandles, LayoutProps, HeaderLayoutProps, BodyLayoutProps, NormalLayoutProps };
export default Alert;
export { Layout as AlertLayout };
